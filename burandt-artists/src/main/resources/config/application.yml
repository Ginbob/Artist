spring:
  jpa:
    hibernate.ddl-auto: update
    show-sql: true
  datasource:
    url: jdbc:mysql://localhost:3306/artist
    username: root
    password: geheim01
  http:
    multipart:
        maxFileSize: 10Mb
        maxRequestSize: 30Mb